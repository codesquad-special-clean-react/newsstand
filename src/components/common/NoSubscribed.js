import * as Styled from './NoSubscribed.style';

const NoSubscribed = () => {
    return (
        <Styled.Div>
            구독된 언론사가 없습니다.😂
            '전체 언론사' 메뉴를 통해 언론사를 구독해주세요.
        </Styled.Div>
    );
};

export default NoSubscribed;