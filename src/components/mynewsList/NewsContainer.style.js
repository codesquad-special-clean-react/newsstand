import styled from "styled-components";
import size from "../../utils/size";

const NewsContainer = styled.div`
    display: flex;
    width: ${size.listview.newsWidth};
`;

export { NewsContainer };