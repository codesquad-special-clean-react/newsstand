import styled from "styled-components";

const HoveredPressContainer = styled.div`
    cursor: pointer;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100%;
`;

const Button = styled.button`
    display: inline-block;
    cursor: pointer;
`;

export { HoveredPressContainer, Button };